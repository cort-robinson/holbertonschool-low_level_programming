#include <stdio.h>

/**
 * main - outputs a string with printf
 * Return: 0 if code returns no errors
 */

int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
