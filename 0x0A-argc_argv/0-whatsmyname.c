#include "holberton.h"
#include <stdio.h>

/**
 * main - Prints the program's name.
 * @argc: Count of arguments.
 * @argv: Arguments used to call program.
 * Return: 0 if no errors.
 */

int main(int argc, char *argv[])
{
	printf("%s\n", argv[0]);
	return (0);
}
